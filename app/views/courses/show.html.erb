<h1>Courses#show</h1>
<p>Find me in app/views/classes/show.html.erb</p>
  <%= render partial: 'course', locals: { course: @course } %>
  <%= link_to "Edit Course", edit_course_path(@course) %>
  <%= link_to "Destroy Course", course_path(@course), method: :delete %>
